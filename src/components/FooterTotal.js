import React from "react";
import { View, Text, Platform } from 'react-native';

const FooterTotal = ({ subTotal, platformFee, total, onPress }) => {
    return(
        <View>
            <View
            >
                <Text>Hello</Text>
            </View>
        </View>
    )
}